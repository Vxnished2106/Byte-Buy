/**
 * DTO de respuesta para una etiqueta.
 */
export class ResponseEtiquetaDto {
  /** ID único de la etiqueta. */
  etiqueta_id: number;

  /** Nombre de la etiqueta. */
  etiqueta_nombre: string;
}
